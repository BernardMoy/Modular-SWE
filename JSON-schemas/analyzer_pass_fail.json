{
  "type": "object",
  "properties": {
    "result": {
      "description": "A single string, either pass or fail, based on whether the current design is suitable for implementation that ensures good code quality and long term maintainability.",
      "type": "string",
      "enum": ["pass", "fail"]
    },
    "reason": {
      "description": "Why the current design pass or fails.",
      "type": "string"
    },
    "expected_improvements": {
      "description": "Explain how would the code quality and maintainability improve after implementing the suggested improvements.",
      "type": "string"
    }
  },
  "required": ["result", "reason"],
  "additionalProperties": false
}

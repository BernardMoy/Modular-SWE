{
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "module_name": {
        "description": "The name of the module.",
        "type": "string"
      },
      "smell": {
        "description": "A short sentence describing the code smell identified.",
        "type": "string"
      },
      "improvement_instruction": {
        "description": "Improvement instructions on mitigating the code smell.",
        "type": "string"
      },
      "reason": {
        "description": "Reason that this improvement is necessary based on a reasonable and practical scenario when the code evolves, showing the maintainability and quality issues."
      }
    },
    "required": ["module_name", "smell", "improvement_instruction", "reason"],
    "additionalProperties": false
  }
}

{
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "name": {
        "description": "Name of the test.",
        "type": "string"
      },
      "message": {
        "description": "A human readable message describing why the test failed, including the difference between what is expected and what is observed.",
        "type": "string"
      }
    },
    "required": ["name", "message"],
    "additionalProperties": false
  }
}

{
  "type": "object",
  "properties": {
    "result": {
      "description": "Either pass or fail, on whether the current design is suitable for implementation that can preserves code quality and maintainability when new features are added later.",
      "type": "string",
      "enum": ["pass", "fail"]
    },
    "reason": {
      "description": "Why the current design pass or fails.",
      "type": "string"
    },
    "expected_improvements": {
      "description": "Explain how would the code quality and maintainability improve after implementing the suggested improvements.",
      "type": "string"
    }
  },
  "required": ["result", "reason"],
  "additionalProperties": false
}

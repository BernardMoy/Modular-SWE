{
  "type": "array",
  "items": {
    "oneOf": [
      {
        "type": "object",
        "description": "A keep module with a previous concrete implementation, only record the name, path and its responsibility.",
        "properties": {
          "type": {
            "type": "string",
            "const": "keep",
            "description": "'keep' type should be used only when there is a concrete implementation from the previous code, NOT TO BE USED when new modules are not updated from later designs."
          },
          "module_name": {
            "description": "The name of the module that follows pydeps convention.",
            "type": "string"
          },
          "responsibility": {
            "description": "A sentence describing the single responsibility of this module.",
            "type": "string"
          }
        },
        "required": ["type", "module_name", "responsibility"],
        "additionalProperties": false
      },
      {
        "type": "object",
        "description": "A new or changed module with more detailed designs.",
        "properties": {
          "type": {
            "type": "string",
            "enum": ["changed", "new"],
            "description": "'changed' means that the module MUST HAVE a prior implementation and its design has changed from that implementation. 'new' means the module DOES NOT have a previous implementation or when a new module has been updated."
          },
          "module_name": {
            "description": "The name of the module that follows pydeps convention.",
            "type": "string"
          },
          "responsibility": {
            "description": "A sentence describing the single responsibility of this module.",
            "type": "string"
          },
          "non_responsibilities": {
            "description": "A list of things that this module explicitly does not do, to ensure single responsibility principle.",
            "type": "array",
            "items": { "type": "string" }
          },
          "inputs": {
            "description": "A list of data structures for the inputs of this module.",
            "type": "array",
            "items": { "$ref": "#/$defs/dataObject" }
          },
          "outputs": {
            "description": "A list of data structures for the outputs of this module.",
            "type": "array",
            "items": { "$ref": "#/$defs/dataObject" }
          },
          "public_interface": {
            "description": "The public interface that is exposed to other modules, represented by a list of public methods.",
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "name": {
                  "description": "Method name.",
                  "type": "string"
                },
                "description": {
                  "description": "A description of what this method does.",
                  "type": "string"
                },
                "parameters": {
                  "type": "array",
                  "items": {
                    "type": "object",
                    "properties": {
                      "name": { "type": "string" },
                      "type": { "type": "string" },
                      "optional": { "type": "boolean" }
                    },
                    "required": ["name", "type", "optional"],
                    "additionalProperties": false
                  }
                },
                "returns": {
                  "description": "The return type of the method.",
                  "type": "string"
                },
                "preconditions": {
                  "description": "Conditions that should hold true before execution of the method.",
                  "type": "array",
                  "items": { "type": "string" }
                },
                "postconditions": {
                  "description": "Conditions that should hold true after the method returns.",
                  "type": "array",
                  "items": { "type": "string" }
                }
              },
              "required": [
                "name",
                "description",
                "parameters",
                "returns",
                "preconditions",
                "postconditions"
              ],
              "additionalProperties": false
            }
          }
        },
        "required": [
          "type",
          "module_name",
          "responsibility",
          "non_responsibilities",
          "inputs",
          "outputs",
          "public_interface"
        ],
        "additionalProperties": false
      }
    ]
  },
  "$defs": {
    "dataObject": {
      "type": "object",
      "properties": {
        "name": {
          "description": "Name of the data object.",
          "type": "string"
        },
        "type": {
          "description": "Type of the data object.",
          "type": "string"
        },
        "fields": {
          "description": "Fields of the data object.",
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "name": { "type": "string" },
              "type": { "type": "string" },
              "optional": { "type": "boolean" }
            },
            "required": ["name", "type", "optional"],
            "additionalProperties": false
          }
        }
      },
      "required": ["name", "type", "fields"],
      "additionalProperties": false
    }
  }
}

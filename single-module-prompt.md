## A modified prompt from the SCB (slopCodeBench) paper

#### The root folder is omitted, plus rules are added.

---

Implement a program that 100% solves the specification.
That is all you need to do.
Use a virtual environment and ensure that a
'requirements.txt' is present with any dependencies
you need to solve the problem.

You are working on the following issue:
Issue path: scb-problems/cfgpipe/checkpoint_1.md
Issue implementation path: scb-problems/cfgpipe/implementations/checkpoint_1

Rules:

- You are only allowed to reference the above issue document and the config.yaml file under the same problem folder.
- The entrypoint file must be named {{entry_file}}.py where entry_file refers to the field of the same name under the config.yaml file.
